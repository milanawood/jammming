import React from "react";
import { create } from "react-test-renderer";

describe("Spotify functionality", () => {
    test("savePlaylist", () => {
        const component = create(<Spotify />);
    })
})